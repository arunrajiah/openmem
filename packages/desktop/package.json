{
  "name": "@openmem/desktop",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "OpenMem desktop app (Tauri 2 shell around the companion server)",
  "scripts": {
    "predev": "pnpm --filter @openmem/web build && pnpm --filter @openmem/companion build && pnpm --filter @openmem/companion start",
    "prebuild": "pnpm --filter @openmem/web build && pnpm --filter @openmem/companion build && pnpm --filter @openmem/desktop build:sidecar",
    "build:sidecar": "node scripts/build-sidecar.mjs",
    "dev": "tauri dev",
    "build": "tauri build",
    "icon": "tauri icon"
  },
  "devDependencies": {
    "@tauri-apps/cli": "^2.1.0"
  }
}
